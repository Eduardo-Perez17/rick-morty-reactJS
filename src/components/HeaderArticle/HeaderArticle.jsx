import { HOME_IMAGE } from '../../utils/images';
import { Image } from '../Image';

const HeaderArticle = () => {
  return <Image srcImage={HOME_IMAGE.homeImage} altImage={HOME_IMAGE.homeImage} />;
};

export default HeaderArticle;
